name: Renovate Dependencies
on:
  schedule:
    - cron: "0 0 * * *"

  workflow_dispatch:
    inputs:
      dry_run:
        description: "Enable to perform a dry run"
        required: true
        default: "false"
        type: choice
        options:
          - "true"
          - "false"
      rebase_all:
        description: "Force rebase all PRs"
        required: false
        default: "false"
        type: choice
        options:
          - "true"
          - "false"
      recreate_closed:
        description: "Recreate closed PRs"
        required: false
        default: "false"
        type: choice
        options:
          - "true"
          - "false"

permissions:
  contents: write
  pull-requests: write
  issues: write


jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
      - name: Self-hosted Renovate
        uses: renovatebot/github-action@a3c115cd6676c8a5bc72f9715f108759e570daf5 # v43.0.19
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          configurationFile: renovate.json
        env:
          RENOVATE_DRY_RUN: ${{ github.event_name == 'schedule' && 'false' || github.event.inputs.dry_run }}
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          RENOVATE_RECREATE_CLOSED: ${{ github.event.inputs.recreate_closed || 'false' }}
          RENOVATE_REBASE_STALE_PRS: ${{ github.event.inputs.rebase_all || 'false' }}
          RENOVATE_REQUIRE_CONFIG: 'optional'
          LOG_LEVEL: debug
